<!DOCTYPE html>
<html>
	<head>
		<title>Admin</title>
		<style>
body {
	font-family: monospace;
}

#userlist {
	overflow-y: scroll;
	max-height: 67vh;
}

		</style>
	</head>
	<body>
		<h1>Administrator</h1>
		<p>Welcome to the admin panel. Currently it does not actually do anything, just shows stuff.</p>
		
		<h3>Users</h3>
		<ul id="userlist">
		<% for (let i = 0; i < users.length; i++) { %>	
			<li><%= users[i].username %> (<%= users[i].rating || "no rating" %>)</li>
		<% } %>
		</ul>
		
		<h3>Games in Progress</h3>
		<table>
			<thead>
				<tr>
					<th>ID</th>
					<th colspan="2">Players</th>
				</tr>
			</thead>
			<tbody>
			<% for (let i = 0; i < games.length; i++) {
				const game = games[i]; %>
				<tr>
					<td>
						<a href="/game/<%= game.id %>"><%= game.id %></a>
					</td>
				<% for (let j = 0; j < players.length; j++) { %>
					<td><%= players[j] %></td>
				<% } %>
				</tr>
			<% } %>
			<% if (games.length === 0) { %>
				<tr>
					<td colspan="3" align="center">No games being played at this time</td>
				</tr>
			<% } %>
			</tbody>
		</table>
		
		<%- include("footer") %>
	<script src="/scripts/vendor.js?7832aad1b56b85482c54"></script></body>
</html>