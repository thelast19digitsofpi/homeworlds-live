<!DOCTYPE html>
<html>
	<head>
		<%- include("headElement") %>
		<title>Change Password</title>
	</head>
	<body>
		<%- include("header") %>
		<div class="container">
			<h3>Change Password <strong class="small text-dark">for: <%= userInfo.username %></strong></h3>
			<form id="change-password-form" method="post" action="/changePassword">
				<% if (typeof error !== 'undefined') { _%>
				<h5 class="text-warning"><%= error %></h5>
				<% } %>
				<div class="form-group">
					<label for="oldPassword">Old Password</label>
					<input type="password" name="oldPassword" class="form-control" aria-describedby="login-username" />
				</div>
				<p class="form-text mb-1">Passwords must be 12-99 characters.</p>
				<p class="text-danger"><strong>DO NOT RE-USE A PASSWORD YOU USED ON ANOTHER SITE</strong>. Again, I'm not a security expert.</p>
				<div class="form-group">
					<label for="newPassword">New Password</label>
					<input type="password" name="newPassword" class="form-control" aria-describedby="login-password" />
				</div>
				<div class="form-group">
					<label for="confirmPassword">Confirm New Password</label>
					<input type="password" name="confirmPassword" class="form-control" aria-describedby="login-password" />
				</div>
				<input type="submit" value="Change Password" class="btn btn-primary btn-lg" />
			</form>
		</div>
		<%- include("footer") %>
	<script src="/scripts/vendor.js?020324f1140531b2f379"></script></body>
</html>